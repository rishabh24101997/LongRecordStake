class POSITVItem {
  // const POSITVItem({this.title, this.url, this.isNew});

  POSITVItem(String title, String url) {
    this.title = title;
    this.url = url;
  }

  String url;
  String title;
  // final bool isNew;
}
